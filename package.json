{
  "author": "Sean Shen",
  "bin": {
    "pb": "./bin/pbcli.js"
  },
  "description": "This is the cli tool for pbxt",
  "directories": {
    "lib": "./lib"
  },
  "license": "ISC",
  "name": "pbxt-cli",
  "scripts": {
    "build": "babel lib --out-dir dist --watch --source-maps inline",
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "node_modules/.bin/babel-node-debug ./bin/pbcli.js"
  },
  "version": "1.0.3",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "babel": {
    "presets": [
      [
        "es2015"
      ]
    ]
  }
}
